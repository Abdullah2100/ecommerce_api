services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 9999:5000
    environment:
      - ASPNETCORE_ENVIROMENT=Production
      - ASPNETCORE_URLS=http://*:5000
    volumes:
      - ./images:/app/images
